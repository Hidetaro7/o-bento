<%= simple_form_for(@bento) do |f| %>
  <%= f.error_notification %>

  <div class="inputs">
    <%= f.input :name %>
    <%= f.input :url %>
  </div>

  <div class="actions">
    <%= f.button :submit %>
  </div>
<% end %>
